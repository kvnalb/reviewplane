{
  "name": "reviewplane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.12.0 <23"
  },
  "scripts": {
    "dev": "npm run dev --workspace @reviewplane/landing",
    "build:sdk": "npm run build --workspace @reviewplane/core && npm run build --workspace @reviewplane/react && npm run build --workspace @reviewplane/vite",
    "build": "npm run build:sdk && npm run build --workspace reviewplane && npm run build --workspace @reviewplane/landing",
    "typecheck": "npm run build:sdk && npm run typecheck --workspace reviewplane && npm run typecheck --workspace @reviewplane/landing",
    "test": "npm run build:sdk && npm run test --workspace @reviewplane/core && npm run test --workspace @reviewplane/react && npm run test --workspace @reviewplane/vite && npm run test --workspace reviewplane",
    "test:e2e": "npm run test:e2e --workspaces --if-present"
  }
}
