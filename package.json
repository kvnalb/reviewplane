{
  "name": "@reviewplane/monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tests/e2e"
  ],
  "engines": {
    "node": ">=22.12.0 <23"
  },
  "scripts": {
    "build:packages": "npm run build --workspace @reviewplane/core && npm run build --workspace @reviewplane/vite && npm run build --workspace @reviewplane/react && npm run build --workspace reviewplane",
    "dev": "npm run build:packages && npm run dev --workspace @reviewplane/landing",
    "build": "npm run build:packages && npm run build --workspace @reviewplane/landing",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:e2e": "npm run test:e2e --workspace @reviewplane/e2e --if-present",
    "test:pack": "npm run test:pack --workspace reviewplane",
    "pack:packages": "npm run build:packages && npm pack --workspace @reviewplane/core --workspace @reviewplane/vite --workspace @reviewplane/react --workspace reviewplane --pack-destination /tmp/reviewplane-packs"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
